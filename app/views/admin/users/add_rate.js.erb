<% if @success %>
  $('.modal.in').modal('toggle');
  window.location = window.location;
<% else %>
  $('#addRate').find('.modal-body')
          .html('<%= escape_javascript render(partial: 'rate_form') %>');
<% end %>