<% if @success %>
  window.location = window.location;
<% else %>
  $('#addProject').find('.modal-body')
          .html('<%= escape_javascript render(partial: 'project_form', locals: project_form_params) %>');
<% end %>